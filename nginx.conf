server {
    listen 80;
    server_name localhost;

    # Define the location for /home
    location /home {
        alias /usr/share/nginx/html; # Assuming index.html is in this directory
        index index.html;
    }

    # Redirect all other requests to /home
    location / {
        return 302 /home;
    }
}
